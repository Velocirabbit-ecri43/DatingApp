import React from "react";
import { render, screen } from "@testing-library/react";

import MatchesContainer from "../src/containers/MatchesContainer";

const initialState = {
  matches: {
    matches: [{}],
  },
};
